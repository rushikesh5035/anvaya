import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["nonregressively-hyperdicrotic-arya.ngrok-free.dev"],
  cacheComponents: true,
};

export default nextConfig;
